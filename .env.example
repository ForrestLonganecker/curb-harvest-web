NEXT_PUBLIC_API_URL=http://localhost:3001

# Mapbox account > Tokens. Used client-side for geocoding + the map on the
# create-crop form, so this must be a public token restricted to your domains.
NEXT_PUBLIC_MAPBOX_TOKEN=
